import 'package:get/get.dart';

class Dimension {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;
  //height
  static double h10 = screenHeight / (screenHeight / 10);

  static double h8 = screenHeight / (screenHeight / 8);
  static double h2 = screenHeight / (screenHeight / 2);
  static double h7 = screenHeight / (screenHeight / 7);
}
